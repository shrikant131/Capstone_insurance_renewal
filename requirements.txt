# Core Data Science
numpy>=1.21.0
pandas>=1.3.0

# Machine Learning
scikit-learn>=1.0.0
xgboost>=1.5.0
lightgbm>=3.3.0
--only-binary=:all: pyarrow

# Deep Learning (optional, comment if installation issues)
tensorflow-macos>=2.8.0; sys_platform == 'darwin' and platform_machine == 'arm64'  # For M1/M2 Macs
tensorflow>=2.8.0; sys_platform != 'darwin' or platform_machine != 'arm64'  # For other platforms
torch>=1.10.0

# ML Utilities
imbalanced-learn>=0.8.0
joblib>=1.1.0
category_encoders>=2.3.0
optuna>=2.10.0
shap>=0.40.0

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0

# Web Framework
streamlit>=1.10.0
fastapi>=0.68.0
pydantic>=1.8.0

# Utilities
requests>=2.26.0

# Optional Models (comment if installation issues)
pytorch-tabnet>=3.1.0